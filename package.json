{
	"name": "clubpetro",
	"version": "1.0.0",
	"main": "index.js",
	"private": true,
	"scripts": {
		"dev": "yarn workspace @project/proxy dev",
		"build": "yarn npm-run-all program:build auth:build",
		"proxy:start": "yarn workspace @project/proxy start",
		"proxy:build": "yarn workspace @project/proxy build",
		"proxy:export": "yarn workspace @project/proxy export",
		"proxy:deploy": "yarn proxy:build && yarn proxy:export",
		"auth:start": "yarn workspace @project/auth start",
		"auth:build": "yarn workspace @project/auth build",
		"auth:e2e": "yarn workspace @project/auth-e2e cypress",
		"storybook:start": "yarn workspace @project/uikit-storybook start"
	},
	"workspaces": {
		"packages": [
			"packages/*",
			"packages/**/*"
		]
	},
	"devDependencies": {
		"@project/eslint-config": "*",
		"npm-run-all": "^4.1.5"
	},
	"license": "MIT"
}
